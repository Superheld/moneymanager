The shared screen top — context bar (date + scenario) above title/subtitle and right-side controls.

```jsx
<PageHeader title="Töpfe" subtitle="Spartöpfe für Ersatz · Puffer für Überraschungen"
  right={<Button variant="primary" plus>Topf anlegen</Button>} />
```

Every screen starts with this so the app feels of one piece.
