Labelled field row with optional required asterisk and hint. `Input` is the styled field box; `calc` is the dashed-teal computed field.

```jsx
<FormField label="Wiederbeschaffungswert" required>
  <Input value="14.400" suffix="€" />
</FormField>
<FormField label="Kategorie"><Input value="Vermögen" caret /></FormField>
```
