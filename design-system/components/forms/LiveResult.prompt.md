The teal live-computed box inside create dialogs — the signature "math happens in front of you" moment.

```jsx
<LiveResult label="Ansparrate (berechnet)" value="+150 €" sub="14.400 € ÷ 8 Jahre ÷ 12">
  <div style={{marginTop:9,fontSize:12.5,color:'var(--ink-2)'}}>kalkulatorische Abschreibung — keine echte Buchung.</div>
</LiveResult>
```
