Selectable explainer card for teaching choices (Charakter, Topf-Art, Glättung). Lay 2–3 in a row inside a FormField.

```jsx
<RadioCard title="Umschichtung" selected tag={<Pill variant="um">gewählt</Pill>}
  desc="Sparen/Anlegen — Geld wandert ins Vermögen, kein Verbrauch" />
```
