A single sidebar navigation row; active state uses the teal wash and a filled square marker.

```jsx
<NavItem label="Übersicht" active />
<NavItem label="Liquidität" onClick={() => go('liquiditaet')} />
```

Group rows under an uppercase eyebrow label; keep the icon marker subtle.
