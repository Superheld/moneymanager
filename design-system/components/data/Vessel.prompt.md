The Töpfe "Behälter" — fills from the bottom to coverage %, dashed goal line on top. Teal ≥50%, amber below.

```jsx
<Vessel label="Auto-Ersatz" art="Spartopf" value={8640} target={12000} />
```

Pair with a "% gedeckt" line and the monthly Ansparrate (+X €/Mt). The goal grows over time.
